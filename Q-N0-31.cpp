#include <iostream>
using namespace std;
int main()
{
	int weight;
	
   If ( a.weight > b.weight )
		return true;
	else if ( a.weight < b.weight )
		return false;
	else
	{
		if ( a.name < b.name)
			return true;
		else if( a.name > b.name )
			return false;
		else
		{
			if( a.age < b.age)
				return true;
			else if( a.age > b.age )
					return false;
			else
				return true;
		}
   }
   else
   cout<<"Not valid";
} 
